// Local development environment configuration.
//
// This is a static portfolio demo: there is no backend, so there is nothing
// to configure here. See src/app/core/demo-data.store.ts for the in-memory
// / localStorage data layer that stands in for Supabase in the production
// version of this app.
export const environment = {
  production: false,
};
